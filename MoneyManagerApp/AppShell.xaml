<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MoneyManagerApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MoneyManagerApp"
    xmlns:views="clr-namespace:MoneyManagerApp.Views">

    <!--<FlyoutItem
        Title="Home">
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />
    </FlyoutItem>
    <FlyoutItem
        Title="Info">
        <ShellContent
            ContentTemplate="{DataTemplate views:PaymentInfoPage}"
            Route="PaymentInfoPage" />
    </FlyoutItem>-->
    
    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="User"
            ContentTemplate="{DataTemplate views:UserInfoPage}"
            Route="UserInfoPage" />

        <ShellContent
            Title="Bills"
            ContentTemplate="{DataTemplate views:PaymentInfoPage}"
            Route="PaymentInfoPage" />
    </TabBar>

</Shell>
